ALFRED UNIVERSITY v16.3 — FINAL INSTRUCTIONAL DEPTH — FLAT UPLOAD

This is a complete flat GitHub Pages release.
No nested project folder. No frontend build. No Worker or D1 migration.

1. On the existing Alfred site, open Progress and choose Export Backup.
   Save the Recovery / Student Sync Key separately; it is not included in the export.
2. Extract Alfred-University-v16.3-FINAL-INSTRUCTIONAL-DEPTH-FLAT.zip on your computer.
3. Open the existing numberclaw2/alfred-university repository at the root where index.html already lives.
4. Choose Add file > Upload files and select ALL extracted files together.
   Do not upload the ZIP itself and do not create a wrapper directory.
5. Commit the replacements. Keep the existing GitHub Pages branch/root setting and site address.
6. After Pages finishes deploying, close old Alfred tabs and reopen the normal site URL.
   Release Notes should show v16.3 at the top: “Final Instructional Depth — The Lesson Becomes the Professor.”

QUICK CHECK AFTER UPLOAD
- At 100% zoom, open More and verify every destination remains reachable.
- Open Learn / Classroom. A lesson should render as one integrated lesson: subject-specific instruction, an instructional visual when useful, worked examples, guided practice, a concrete independent scenario, misconceptions/repairs, knowledge checks, and any required semantic evidence task—without the old stacked instructional-completion layer.
- Open Assessments. The page should report 223 active reviewed bank items; the 186 v16.1 generic fill questions are historical only and must not appear in selected mastery forms.
- Open Standards. Current rows should show a v16.3 subject-specific teaching route and semantic task ID.
- Open Calendar, switch Month / Week / Agenda, and open an event. Event dates/identity remain unchanged; malformed corrupted AST/practice wording should be absent.
- Open Documents and confirm the v16.3 Curriculum & Instruction Plan, Lesson-Depth Audit, Semantic Coverage Matrix, and Final Lesson Acceptance Report open correctly.
- Open Progress and confirm existing completion/history is still present.
- If you use Cloud Sync, verify its status on your normal device.

DO NOT CLEAR BROWSER SITE DATA AS A NORMAL UPDATE STEP.
That can erase local progress and the locally stored recovery key. The v16.3 service worker advances the application cache without requiring a learner-data reset.

Cloud Sync remains on protocol 2 and the existing D1 record model. The production Worker URL is unchanged:
https://alfred-university-sync.totallywill13.workers.dev

IMPORTANT ASSESSMENT NOTE
v16.3 preserves the 186 v16.1 generated coverage-fill question IDs for saved-history compatibility but retires them from current mastery evidence. The active auto-graded bank contains the pre-existing substantive reviewed items. Complete row-level coverage is supplied by required topic-specific constructed-response/performance tasks integrated into Classroom; those responses are learner-audited against explicit technical rubrics rather than falsely auto-graded by generic text matching.

The private ETA study guide, Recovery / Student Sync Key, and personal exported backups are not part of this package and must not be committed to the public repository.
