ALFRED UNIVERSITY — v16.3 CURRICULUM / v16.3.2 RUNTIME HOTFIX

This is a surgical root-level GitHub Pages update for an existing v16.3.1 deployment.
It does not change the 62 lessons, 24 labs, 125-event calendar, Cloud Sync protocol 2,
progress keys, or the objective-bank evidence revision (16.2).

BEFORE UPDATING
1. Open Progress and choose Export Backup.
2. Keep the Recovery / Student Sync Key separately. It is not included in the export.
3. Do NOT clear browser site data as a routine update step.

UPLOAD THE v16.3.2 HOTFIX
1. Extract Alfred-University-v16.3.2-RUNTIME-DEPLOYMENT-HOTFIX.zip on your computer.
2. Open numberclaw2/alfred-university at the repository root where index.html lives.
3. Upload every file listed under ADD and REPLACE in AU-ESET-301-v16.3.2-Upload-Manifest.txt.
4. Replace matching filenames. Do not upload the ZIP itself and do not create a wrapper folder.
5. Keep the existing GitHub Pages branch/root setting and site address.
6. No Worker redeployment or D1 migration is required.

VERIFY AFTER GITHUB PAGES FINISHES
- Open build-info.json from the public Pages URL. It must report:
  courseRelease = 16.3
  runtimePatch = 16.3.2
  evidenceRevision = 16.2
  cloudSyncProtocol = 2
- Open the raw service-worker.js in GitHub. It must contain alfred-u-v16-3-2.
- Close old Alfred tabs and reopen the normal Pages URL. The new service worker removes only
  older Alfred Cache Storage entries; it does not erase localStorage, IndexedDB, progress,
  assessment history, notes, or Cloud Sync configuration.
- Open Assessments and run a form. Correct-answer letters should not show an obvious position pattern.
- Open Search and search "v15.8" or "current website". The Academic System Guide may appear,
  but it must be labeled Historical document — not current operating instructions.
- Open Progress and confirm existing completion/history remains present.
- If Cloud Sync is used, verify normal sync status on the usual device.

CURRENT VERSION IDENTITIES
Course curriculum release: 16.3
Runtime patch: 16.3.2
Objective evidence revision: 16.2
Cloud Sync protocol: 2
Production Worker: https://alfred-university-sync.totallywill13.workers.dev

The complete Alfred-University-v16.3-FINAL-INSTRUCTIONAL-DEPTH-FLAT.zip remains the clean-install
curriculum baseline. Do not re-upload all 140+ unchanged files just to apply this runtime patch.
