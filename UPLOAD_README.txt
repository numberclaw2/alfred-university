ALFRED UNIVERSITY v15.2 — CUMULATIVE REPLACEMENT PACKAGE

Upload the files at the ZIP root into the root of the GitHub Pages repository.
Preserve the filenames, spaces, capitalization, PDFs, images, and repository-root layout.

This package includes the cumulative v15.0/v15.1 compatibility and consolidation files plus the v15.2 evidence-backed UX flow pass. It includes the v15.2 shared CSS/JS layer, grouped navigation, context trails, skip link, current-week placement, service-worker cache update, README/release note, and the existing data/state/PDF changes from the preceding releases.

After deployment:

1. Open the site once online so the v15.2 service worker can install.
2. If a previously open tab still shows the old layout, close/reopen it or use a hard refresh.
3. Test Home, Study, Week, Calendar, Practice, Progress, Mastery, More, Release Notes, and one assessment route.
4. Confirm an existing Progress record and Cloud Sync connection remain available before clearing anything. Do not clear browser storage as a cache-refresh method.

The UX audit and static QA report are included as reference files. A full browser screenshot pass was not claimed because the QA environment did not have a usable Chromium binary.
